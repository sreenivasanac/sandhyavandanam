# app

Vite + React + TypeScript + Tailwind PWA. See the repo root README for the project.

```bash
pnpm install
pnpm dev      # http://localhost:5173
pnpm test     # node --test (no framework)
pnpm build    # dist/ (static, PWA)
```
